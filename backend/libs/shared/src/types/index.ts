export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum E_Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum E_OrderStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
}
